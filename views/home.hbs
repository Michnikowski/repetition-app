<!DOCTYPE html>
<html lang="en">

  {{> head}}

  <body>

    <div class="container">
      {{> navbar}}
      {{#each letters}}
        <div>{{letter}}</div>
      {{/each}}
    </div>

  </body>

</html>
